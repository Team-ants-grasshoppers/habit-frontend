// src/components/layout/Header.tsx
import { Link } from 'react-router-dom';

const Header = () => {
  return (
    <header>
      <Link to="/">HABIT</Link>
    </header>
  );
};

export default Header;
